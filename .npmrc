# GitHub Packages Registry Configuration
@artemkdr:registry=https://registry.npmjs.org
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
always-auth=true

# Always use exact versions for workspace dependencies
save-exact=true

# Use Bun for package management
package-manager=bun